# Update a virtualSelectInput label after a language change

Update a virtualSelectInput label after a language change

## Usage

``` r
update_epishiny_virtual_select_label(session, inputId, label)
```

## Arguments

- session:

  Shiny session.

- inputId:

  Namespaced input id.

- label:

  English label key.
