% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_launch.R
\name{epi_dashboard}
\alias{epi_dashboard}
\title{Create a custom epishiny dashboard}
\usage{
epi_dashboard(
  df,
  modules = c("place", "time", "person"),
  include_filter = TRUE,
  title = "epishiny dashboard",
  theme = bslib::bs_theme(),
  col_widths = NULL,
  row_heights = 1,
  ...
)
}
\arguments{
\item{df}{Data frame or tibble of patient level or aggregated data.}

\item{modules}{Character vector specifying which modules to include and their order in the layout.
Valid values are \code{"time"}, \code{"place"}, and \code{"person"}. Default is \code{c("place", "time", "person")}.
The order of the vector determines both which modules appear and their layout order.
Examples:
\itemize{
\item \code{c("time", "place", "person")} - All modules with time first
\item \code{c("place", "time")} - Only map and time series (no demographics)
\item \code{c("time")} - Only time series module
}}

\item{include_filter}{Logical. Include the filter sidebar? Default TRUE.}

\item{title}{Character. Title for the dashboard. Default "epishiny dashboard".}

\item{theme}{A bslib theme object to customize the appearance of the dashboard. Default is \code{\link[bslib:bs_theme]{bslib::bs_theme()}}.}

\item{col_widths}{Numeric vector specifying the column widths for the layout. If NULL (default),
widths are automatically assigned based on number of modules. Use in combination with \code{modules}
to control layout. Example: \code{col_widths = c(12, 7, 5)} with \code{modules = c("time", "place", "person")}
puts time on full width row, then place (7 cols) and person (5 cols) on second row.}

\item{row_heights}{Numeric vector specifying the row heights for the layout. Default is 1 (equal heights).}

\item{...}{Named arguments to pass to the individual module UI and server functions.
Arguments will be matched to their appropriate modules. See \code{\link[=time_ui]{time_ui()}}, \code{\link[=time_server]{time_server()}},
\code{\link[=place_ui]{place_ui()}}, \code{\link[=place_server]{place_server()}}, \code{\link[=person_ui]{person_ui()}}, \code{\link[=person_server]{person_server()}}, \code{\link[=filter_ui]{filter_ui()}}, \code{\link[=filter_server]{filter_server()}}
for details on available arguments.}
}
\value{
A shiny app object that can be passed to \code{\link[shiny:runApp]{shiny::runApp()}} or run directly if in interactive mode.
}
\description{
Build a complete epishiny dashboard with your choice of modules (time, place, person, filter).
This function generates a shiny app with the selected modules and handles all the
boilerplate UI and server code.
}
\examples{
\dontrun{
library(epishiny)
data("df_ll")
data("sf_yem")

# Setup geo data
geo_data <- geo_layer(
  layer_name = "Governorate",
  sf = sf_yem$adm1,
  name_var = "adm1_name",
  pop_var = "adm1_pop",
  join_by = c("pcode" = "adm1_pcode")
)

# Create dashboard with all modules (default)
app <- dashboard(
  df = df_ll,
  date_vars = c("Date of notification" = "date_notification"),
  group_vars = c("Sex" = "sex_id"),
  geo_data = geo_data,
  age_var = "age_years",
  sex_var = "sex_id",
  male_level = "Male",
  female_level = "Female"
)

# Time on top row, place and person below
app_time_focus <- dashboard(
  df = df_ll,
  title = "Time Series Focus",
  modules = c("time", "place", "person"),
  col_widths = c(12, 7, 5),
  date_vars = c("Date of notification" = "date_notification"),
  group_vars = c("Sex" = "sex_id"),
  geo_data = geo_data,
  age_var = "age_years",
  sex_var = "sex_id",
  male_level = "Male",
  female_level = "Female"
)

# Only time and person modules (no map)
app_simple <- dashboard(
  df = df_ll,
  title = "Time & Demographics",
  modules = c("time", "person"),
  date_vars = c("Date of notification" = "date_notification"),
  age_var = "age_years",
  sex_var = "sex_id",
  male_level = "Male",
  female_level = "Female"
)

# Run the dashboard
if (interactive()) {
  shiny::runApp(app_time_focus)
}
}

}
